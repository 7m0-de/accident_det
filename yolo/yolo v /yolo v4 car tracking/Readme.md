Car Tracking Module

This module detects and tracks cars in a video using YOLO and tracking.

Features
Detect cars only
Assign unique ID for each car
Track cars across frames
How to Run
Install dependencies:
pip install ultralytics opencv-python
Run:
python car_tracking.py
Output
Video with bounding boxes
Each car has unique ID
